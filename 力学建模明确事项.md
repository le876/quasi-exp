# 力学建模明确事项

本文记录当前已经确认的绳路由、分段受力口径，以及下一步需要实现/验证的张力分层求解思路。后续改动 `QuasiStaticModel`、`tension_transmission`、PSO 或 canonical allocator 时，以本文口径为优先参考。

## 1. 已确认的绳路由

机器人共有 30 个圆盘，按 10 个圆盘一段划分为三段：

- 第 1 段 / 第一关节：`Disk 1..10`
- 第 2 段 / 第二关节：`Disk 11..20`
- 第 3 段 / 第三关节：`Disk 21..30`

12 根绳的终止盘号采用论文 Eq.(6) 的口径：

- `j in {1,2,11,12}` 终止于第 10 盘
- `j in {3,4,9,10}` 终止于第 20 盘
- `j in {5,6,7,8}` 终止于第 30 盘

这一定义是正确口径。论文 Eq.(44) 中的分段绳编号集合不能简单理解为“该段只受这些绳影响”，因为长绳穿过近端段时仍会通过孔摩擦对近端圆盘施加力/力矩。

## 2. 每段主控绳与穿越摩擦

三段的主控关系与穿越摩擦关系如下。

第一关节：

- 主控第一关节角的绳：`{1,2,11,12}`
- 但第一关节 `Disk 1..10` 仍受到全部 12 根绳影响
- 原因：控制第二、第三关节的 8 根远端绳也穿过第一段，穿越第一段圆盘孔时会产生摩擦力和相应力矩

第二关节：

- 主控第二关节角的绳：`{3,4,9,10}`
- 第二关节 `Disk 11..20` 仍受到 `{3,4,5,6,7,8,9,10}` 影响
- 原因：第三关节绳 `{5,6,7,8}` 穿过第二段，仍会在第二段产生孔摩擦和力矩

第三关节：

- 主控第三关节两个角的绳：`{5,6,7,8}`
- 第三关节 `Disk 21..30` 只受到 `{5,6,7,8}` 这四根终止于第 30 盘的绳直接影响

因此，在残差建模中，某个关节 `i` 是否受到某根绳 `j` 影响，应按“该绳是否穿过该关节所在圆盘段”判断，即当前实现中的 `end_disk_by_j[j] >= i` 是正确方向。

## 3. 当前实现与该口径的关系

当前配置中的 `cables.end_disk_by_j` 已经采用 Eq.(6) 终止盘号：

- `1,2,11,12 -> 10`
- `3,4,9,10 -> 20`
- `5,6,7,8 -> 30`

当前 `QuasiStaticModel.build_cache()` 中的 `active_ij` 逻辑为：

```text
active[i, j] = true if end_disk_by_j[j] >= i
```

这等价于：

- `Disk 1..10`：12 根绳均参与
- `Disk 11..20`：8 根绳参与，即 `{3,4,5,6,7,8,9,10}`
- `Disk 21..30`：4 根绳参与，即 `{5,6,7,8}`

该逻辑符合当前确认的物理口径。

## 4. 需要新增研究的分层张力求解思路

当前 PSO/canonical 方法是在给定完整 `theta[30]` 后，一次性求 12 路基座张力。由于多根绳跨段穿越并带有摩擦传递，同一个 `theta` 下可能存在多组满足残差阈值的张力分配，导致原始 PSO 多 seed 结果不一致。

新的求解思路是按远端到近端分层：

1. 先求第三关节四根绳 `{5,6,7,8}` 的张力
   - 目标主要满足第三段 `Disk 21..30` 的准静态平衡
   - 但求出的张力应记录在第 20 盘处或第 20/21 盘界面处，因为这些绳从第 20 盘继续向基座穿过第二、第一段时仍会因孔摩擦发生张力变化

2. 再求第二关节四根主控绳 `{3,4,9,10}` 的张力
   - 同时考虑第三关节绳 `{5,6,7,8}` 穿过第二段造成的摩擦力/力矩
   - 目标满足第二段 `Disk 11..20` 的准静态平衡
   - 需要把第二段求得的张力继续向第 10 盘/第一段边界传递

3. 最后求第一关节四根主控绳 `{1,2,11,12}` 的张力
   - 同时考虑全部 12 根绳在第一段 `Disk 1..10` 的穿越摩擦
   - 目标满足第一段准静态平衡，并得到最终 12 路基座张力标签

这个远端到近端的分层求解更接近实际驱动链路：远端关节的绳必须穿过近端段，近端段不仅受本段主控绳影响，也受远端绳穿越摩擦影响。该方法预期会比一次性 12 维 PSO 更稳定，因为每一步的主控变量更少，且可以把已求远端张力作为近端求解的已知穿越载荷。

## 5. 后续实现前必须明确的问题

在实现分层张力求解之前，需要明确以下技术细节：

1. 第三段求解变量到底定义在哪个位置
   - 可选：定义为第 20/21 盘界面处张力，再向远端递推到第 30 盘
   - 或定义为基座张力中的 `{5,6,7,8}`，但这样会重新把近端摩擦耦合带入第三段求解，不利于分层

2. 张力传递方向
   - 当前 `transmit_tensions()` 从基座 `F_CT_0j` 向远端递推
   - 分层求解可能需要反向传递：从远端段边界张力向近端回推，或按段局部重新定义递推起点

3. 摩擦 Case 1/2 的历史方向
   - 当前用总绳长变化 `L(theta)-L(0)` 选择 Case
   - 分层求解最好按每段绳长变化或加载路径选择 Case，否则远端段与近端段可能使用同一个全局 Case，物理含义偏粗

4. 每段 residual 的切片
   - 第三段：优先约束 `i=21..30`
   - 第二段：优先约束 `i=11..20`
   - 第一段：优先约束 `i=1..10`
   - 需要决定是否在每段内加入边界连续/全局 residual 的二级惩罚

5. canonical tie-break 是否仍需要
   - 分层求解可能显著提升一致性，但仍可能存在冗余解
   - 即使采用分层，也建议保留 deterministic tie-break，例如最小最大张力、最接近参考张力、或与上一段边界张力连续

## 6. 建议的验证顺序

1. 先不生成大数据集，只抽取 20 到 50 个标准扫描 `theta`
2. 对同一个 `theta` 做多 seed 对比：
   - 当前一次性 PSO
   - 当前 PSO + canonical
   - 新分层求解
3. 对比指标：
   - 每段 residual RMS
   - 全局 residual RMS
   - 12 路基座张力 pairwise MAE / p95
   - 张力是否越界
   - 计算耗时
4. 如果分层求解的多 seed 一致性明显更好，再生成 2k/10k 数据集重新训练 MLP。

## 7. 当前实现决定（2026-05-25）

本轮先实现“分层张力求解诊断原型”，不直接替换现有 2k/10k/100k 数据集生成流程。目标是用代码验证分层口径是否能改善张力一致性和求解速度，再决定是否接入正式数据生成。

本轮实现采用以下约定：

1. 求解顺序固定为远端到近端：
   - 第三段：先求 `{5,6,7,8}`，残差切片为 `Disk 21..30`
   - 第二段：再求 `{3,4,9,10}`，残差切片为 `Disk 11..20`
   - 第一段：最后求 `{1,2,11,12}`，残差切片为 `Disk 1..10`

2. 分段变量定义为段入口边界张力：
   - 第三段变量定义在 `Disk 20/21` 界面，即 `F_CT_{20,j}` 到 `F_CT_{21,j}` 的段入口近似
   - 第二段变量定义在 `Disk 10/11` 界面
   - 第一段变量定义在 `Disk 0/1` 基座入口

3. 为了先复用当前 Eq.(42) 正向张力传递实现，边界张力会通过单位传递因子换算为基座张力：
   - 先用 `T_base = ones(12)` 计算 `F_unit[i,j]`
   - 若某段入口目标边界张力为 `x_j`，则换算为 `T_base_j = x_j / F_unit[i,j]`
   - 这样可以在不改动 `transmit_tensions()` 的前提下，测试分段变量是否更稳定

4. 每段先使用确定性局部最小二乘：
   - 不使用 PSO 随机 seed
   - 每段只优化本段四根主控绳
   - 已求出的远端绳张力在近端段中保持为已知穿越载荷
   - 每段目标包含本段残差、边界张力参考项和基座张力范数项，作为 deterministic tie-break

5. 本轮不解决的物理细节：
   - 不重新定义每段局部的 Case 1/2
   - 不实现真正的反向摩擦传递公式
   - 不把分段结果直接作为论文最终张力标签

验收重点是：

- 同一 `theta` 重复求解应完全一致或数值误差接近 0
- 平均单点耗时要显著低于当前多 seed PSO + canonical 路径
- 全局 residual 和分段 residual 必须被记录；若全局 residual 不合格，说明需要在该原型后增加全局 polish 或补全反向/分段摩擦模型

## 8. 已接入正式数据生成的记录（2026-05-29）

上述分层张力求解思路已经从诊断原型推进为正式数据标注路径，配置入口为：

- `tension_labeler.method = segmented_canonical`
- `configs/robot_rods_only_standard_100k_segmented_canonical.yaml`

最终 100k 数据集已经生成并通过数据质量、局部连续性、Classic MLP 与 TF MLP 验收。详细结果记录见：

- `segmented_canonical_100k执行记录.md`

关键检索词：

- `segmented_canonical`
- `standard_beta_sweep_100k_segmented_canonical`
- `tension_labeler`
- `张力 MAE`
- `local continuity`

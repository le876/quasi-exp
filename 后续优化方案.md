# 后续优化方案（数据复杂度不足时的“非严格恒曲率”升级）

本文档用于提醒/记录：如果后续实验发现当前数据集（段内严格恒曲率：同一段内奇数关节角恒等、偶数关节角恒等）导致模型泛化不足、误差下降遇到瓶颈，建议升级为“非严格恒曲率”（段内关节角允许小幅变化），并重新生成数据集再训练。

> 当前实现：`beta -> theta` 严格按论文 Eq.(50) 展开（见 `src/quasi_exp/model/sampling.py`），PSO 只优化 12 路基座张力，不会修改 `theta`（见 `src/quasi_exp/opt/pso.py`）。

---

## 触发条件（建议满足任一即考虑升级）

1) MLP 在训练集上已明显收敛，但在验证/测试集上端点误差（FK 回代）出现“长尾”且调参/增大数据量收益很小。
2) 对真实机器人/更高保真仿真的域外测试表现差，怀疑“段内 θ 过于规则”造成分布偏窄。
3) 输出 `theta_i` 在每段内只有两种取值（odd/even），导致网络容易记忆“模板”，对更真实的非恒曲率姿态拟合差。

验收指标建议（可按项目需要调整）：
- 测试集端点回代误差 `p95 <= 15mm` 且 `max <= 50mm`（或与你的论文/实验目标对齐）
- 张力打满比例 `P(max_tension==2000N)` 不长期 > 20%

---

## 方案 A：段内微扰（推荐先做，改动小、风险低）

### 思路
保留论文 Eq.(50) 的 6 维 `beta` 作为“段内主曲率”，在展开成 30 维 `theta` 后，对每段内每个关节施加小扰动，使得：
- 段内 odd/even 不再严格相等，但仍“近似恒曲率”（扰动受控）
- 扰动具有一定平滑性（避免高频抖动造成不真实姿态）

### 实施要点（决策必须固定，否则不可复现）
1) 扰动幅度：例如 `σ = 1° ~ 3°`（弧度约 `0.017~0.052`）
2) 平滑方式（二选一，推荐 2）：
   - 1）独立高斯噪声：`eps_i ~ N(0, σ^2)`（最简单，但不够平滑）
   - 2）AR(1) 平滑噪声：`eps_i = ρ*eps_{i-1} + sqrt(1-ρ^2)*η_i`，`η_i~N(0,σ^2)`（推荐，形状更像真实柔顺体）
3) 约束投影：扰动后强制 `|theta_i| <= theta_max`（如 20°）
4) 记录随机种子：将 `sampling.rng_seed` 与扰动独立 seed 写入 meta，保证可复现

### 需要修改的代码点
- `src/quasi_exp/model/sampling.py`：新增 `beta_to_theta_perturbed(beta, seed, sigma, rho, theta_max)`（不替换原函数，保持向后兼容）
- `scripts/worker_generate_sample.py`：根据 config 选择采样模式（strict / perturbed）
- `configs/`：新增一套带扰动的配置（例如 `robot_rods_only_20deg_penalty_100k_perturbed.yaml`）

### 验收（必须做）
1) 统计段内方差：对每条样本、每段，计算
   - `var({theta_odd})`、`var({theta_even})`
   - 期望：显著 > 0（不再是 0），但不应过大（例如 `p95 <= (3°)^2`）
2) 张力与残差：仍满足
   - `rms_rnorm.max < threshold`
   - `P(max_tension==2000N)` 不显著恶化
3) 数据集可视化抽查：随机抽 20 条，用 FK 画中心线/末端点，确保姿态不出现明显锯齿

---

## 方案 B：把 θ 也纳入优化变量（改动大、收益可能更高）

### 思路
在给定 `beta` 或给定 `p_target` 的情况下，不再强制 Eq.(50)，而是让每个关节角 `theta_1..theta_30` 成为优化变量之一（与 `Tension_1..12` 同时优化或分层优化），并引入“近似恒曲率”的惩罚项：
- 段内 odd/even 方差惩罚（保持近似恒曲率）
- 段内二阶差分惩罚（光滑惩罚，抑制高频摆动）

### 两种落地方式（建议先做 B1）
- B1（更贴近当前数据生成方式）：仍然先采样 `beta`，以 `theta_init = Eq.(50)` 作为初值，然后在优化中允许 `theta` 轻微偏离：
  - 目标：平衡残差 + 张力目标 + “偏离恒曲率/不光滑”的惩罚
  - 输出：优化后的 `theta` 与 `tension`
- B2（更像 IK + statics）：先采样 `p_target`，再优化 `theta,tension` 同时满足：
  - 运动学约束：`FK(theta) ≈ p_target`
  - 准静态残差：`f(theta, tension)=0`
  - 张力目标 + 光滑/方差惩罚

### 目标函数建议（示例，需固定权重）
令 `r(theta,T)` 是 30 维准静态残差（归一化后），`T` 是 12 维张力：
- `J = w_resid * mean(r^2)`
  `+ lambda_max * (max(T)/Tmax)^p`
  `+ w_smooth * Σ_i (theta_{i+1}-2*theta_i+theta_{i-1})^2`
  `+ w_var * (var(theta_odd_seg1)+var(theta_even_seg1)+...seg3)`
  `+ w_dev * mean((theta - theta_init)^2)`（可选：限制偏离 Eq.(50)）

### 验收（必须做）
1) 复现论文 Table2 风格的“段内非严格恒曲率”：检查同一段 even 角不再全相等，但量级仍小
2) 计算代价分解（resid/张力/平滑/方差）在样本上的统计，确保不是某一项被压到极端
3) 与方案 A 对比：端点误差分布、张力分布、可行率/耗时

---

## 重新训练与对比（两方案共用）

1) 用同一套训练脚本/超参数先训练 strict 数据集，再训练 perturbed/optimized 数据集（避免“脚本差异”导致的假提升）。
2) 必须输出以下对比指标：
   - 端点回代误差（FK(pred_theta) vs x,y,z）：mean/p95/max（mm）
   - `theta` RMSE（deg）
   - `tension` RMSE（N）以及打满比例
3) 若 perturbed/optimized 数据集在端点误差与域外表现上稳定更好，则将其作为主数据集版本，并记录版本号/配置文件 hash。


# 数据集复现执行计划（含验收与记录）

目标：基于 `原论文.md` 的流程（DH 运动学 + 准静态平衡 + Monte Carlo + PSO），在本仓库复现论文数据集：输入末端点 `x,y,z`，输出 `θ1..θ30` 与 `Tension1..Tension12`（基座张力，0..2000N），生成 100,000 条有效样本。

> 重要：每一步都必须通过“验收”才能进入下一步，避免跑偏。

---

## Step 0：当前已知/输入信息（来源：你提供的几何与 SolidWorks 质量属性）

### 0.1 结构与长度
- `l0 = 0`
- 普通段长：`l1..l29 = 40mm`
- 末端长度：`l30 = 55.498mm`
- 末端点（论文 `^{30}p_end`）：`[l30,0,0,1]^T`

### 0.2 绳路由（论文 Eq.(6)）
- `j∈{1,2,11,12}` 终止于第 10 盘
- `j∈{3,4,9,10}` 终止于第 20 盘
- `j∈{5,6,7,8}` 终止于第 30 盘

### 0.2.1 绳路由与分段受力口径（已确认）
- `Disk 1..10` 为第一关节，`Disk 11..20` 为第二关节，`Disk 21..30` 为第三关节。
- `{1,2,11,12}` 主要决定第一关节角，但第一关节仍受到其余 8 根远端绳穿越第一段时产生的摩擦力/力矩。
- `{3,4,9,10}` 主要决定第二关节角，但第二关节仍受到 `{5,6,7,8}` 穿越第二段时产生的摩擦力/力矩。
- `{5,6,7,8}` 主要决定第三关节的两个角。
- 因此，准静态残差里某关节是否受某根绳影响，应按“该绳是否穿过该关节所在圆盘段”判断，而不是只按“该绳主要控制哪一段”判断。当前 `end_disk_by_j[j] >= i` 的 active 逻辑符合该物理口径。
- 详细记录见 `力学建模明确事项.md`。

### 0.2.2 分层张力求解思路（待实现/验证）
- 可先求第三关节四根绳 `{5,6,7,8}` 的张力，求解位置建议定义在第 20/21 盘边界附近；随后这些绳向基座穿过第 20 到第 0 盘时，还会因为圆盘孔摩擦发生张力变化。
- 再求第二关节四根主控绳 `{3,4,9,10}` 的张力，并把 `{5,6,7,8}` 穿越第二段产生的摩擦力/力矩作为已知影响纳入第二段平衡。
- 最后求第一关节四根主控绳 `{1,2,11,12}` 的张力，并纳入全部 12 根绳在第一段的穿越摩擦力/力矩，得到最终 12 路基座张力。
- 该远端到近端的分层求解预计会比一次性 12 维 PSO 更稳定，但需要先做多 seed 一致性、全局 residual、耗时和 MLP 指标验证。

### 0.3 孔位模板（第一关节第一圆盘含 12 个孔 = 3 组×4孔）
- group1 / group2 / group3 三组各 4 孔（左上/右上/左下/右下）
- `output_yz = input_yz`（同孔出/入仅 x 位移）
- 奇/偶盘孔位翻符号规则来自 `DH-参数.md`

### 0.4 质量属性（普通关节盘）
- 质量：`0.125 kg`
- COM（SolidWorks 报告坐标系 S）：`(-0.601, -0.754, 11.261) mm`
- 坐标轴换算（你提供）：`x_DH=z_S`, `y_DH=-x_S`, `z_DH=-y_S`

---

## Step 1：生成“可跑通”的机器人 CSV 参数（holes/lengths/disks/material/end_effector）

### 操作
1) 运行模板生成脚本（会把你的孔位/长度/COM 写成 CSV）：
   - `python3 scripts/build_robot_csv_from_templates.py --out-dir data/robot/generated --disk-mass-kg 0.125 --disk-com-sw-mm=-0.601,-0.754,11.261`
2) 使用配置 `configs/robot_generated.yaml`（已指向生成的 CSV）

### 验收
- `python3 scripts/validate_robot_inputs.py --config configs/robot_generated.yaml` 输出 `PASS`
- 直线姿态检查：`p_xyz(theta=0).x ≈ 29*0.04 + 0.055498 = 1.215498 m`

### 记录/假设
- 假设：SolidWorks 报告坐标系原点与 DH `{i}` 原点（转轴中心）一致（无额外平移）
- material.csv 的 E/Iz 若暂时未知可先占位，但生成数据集前必须补齐真实值

---

## Step 2：交叉验证 DH 运动学 + 孔位 + 绳长变化（对齐 theta2rope）

目的：确保我们用论文 Eq.(5) 计算的绳长变化 ΔL 与项目 C++ `theta2rope` 的 ΔR 在趋势/量级上匹配，从而确认 DH/孔位符号约定没有反。

### 操作
1) 运行交叉验证：
   - `python3 scripts/cross_validate_rope_length.py --config configs/robot_generated.yaml --trials 20 --scale 0.2`

### 验收
- `mean(corr) >= 0.95`
- `mean(MAE_mm) <= 2.0 mm`（量级一致即可；若更大，优先检查孔编号映射/odd-even 推断）

### 结果（已完成）
- 发现 `theta_sign=+1` 时 corr≈-1（整体符号相反）
- 采用假设：`kinematics.theta_sign=-1` 后 corr=+1，MAE≈0.874mm
 - 复验（20 trials, scale=0.2）：`mean(corr)=1.000, mean(MAE_mm)=1.085`

### 记录/假设
- 结论：为了与 `theta2rope` 一致，DH 中使用的关节角应取 `θ_model = -θ_raw`
- 该假设已写入 `configs/robot_generated.yaml`：`kinematics.theta_sign=-1`

---

## Step 3：补齐/估计摩擦与材料参数（先跑通，再迭代）

### 操作
- `mu_shaft`、`mu_cable`：先用估计值（已在 config 中给默认）
- 材料参数：补齐 `material.csv` 的 `E_pa` 与 `Iz_m4`

#### 3.1 SolidWorks 导出“真实”E（弹性模量）
- `E` **不是几何量**，SolidWorks 也不会“算出来”，只能来自你给零件/材料库填写的材料属性（或厂家数据表）。
- 在 SolidWorks 中：
  1) 打开 **NiTi 杆**的零件（或装配体里选中该零件）
  2) 右键 `Material -> Edit Material...`
  3) 在材料属性里读取 `Elastic Modulus`（单位通常是 `N/mm^2` = `MPa`）
  4) 写入 `material.csv` 的 `E_pa`：`E_pa = E_(N/mm^2) * 1e6`

#### 3.2 SolidWorks 导出“真实”Iz（截面二次矩 / Area moment of inertia）
> 注意：SolidWorks 的 `Mass Properties` 里给的是**质量转动惯量**（kg·mm² / kg·m²），不是我们在 `material.csv` 里需要的 **截面二次矩**（mm⁴ / m⁴）。

两种可行路径（推荐 A）：
- A（推荐，装配体直接得到 4 根杆的“等效截面二次矩”）：
  1) 在装配体里建立一个与 `x_DH` 垂直的截面平面（例如穿过某个圆盘中心，法向沿 `x_DH`）
  2) 用 `Evaluate -> Section Properties`（或同名功能）对该截面求性质
  3) 读取 `Area Moments of Inertia` 的 `Izz`（单位通常 mm⁴）
  4) 转成 m⁴：`Iz_m4 = Iz_mm4 * 1e-12`
- B（用公式，从杆直径得到单杆 Iz，再按你的“等效模型口径”组合）：
  - 单根圆杆：`I = π d^4 / 64`
  - `d=1.7mm` 时：`I_single ≈ 4.10e-13 m^4`
  - 4 根杆“简单并联”（不考虑平行轴项）：`Iz ≈ 4 * I_single ≈ 1.64e-12 m^4`

#### 3.3 当前工程里对 Iz 的“口径选择”（重要）
你更正：NiTi 杆为**实心圆**，直径 `d=1.6mm`，4 根贯穿全长（长度 1.21m 只影响质量/重力，不影响 `Iz`）。

用“复合截面 + 平行轴项”计算（关于 rods 截面形心轴）时：
- 单杆：`I_single = π d^4 / 64 ≈ 3.217e-13 m^4`
- 面积：`A = π d^2 / 4 ≈ 2.011e-6 m^2`
- 代入你给的 4 个杆孔 (y,z)（mm 量级）后得到：
  - `Iz_true_centroid ≈ 9.297e-10 m^4`

重要澄清：上述“复合截面 + 平行轴项”隐含了一个很强的组合梁假设：

> 4 根杆像一个整体梁一样工作，截面保持平面，杆间由刚性连接约束，弯曲时偏心杆产生显著轴向拉压应变，所以平行轴项 `A*y^2` 全部生效。

这个假设不符合当前机器人结构。当前 4 根 NiTi 杆之间没有刚性连接约束，弯曲时不会彼此直接产生轴向拉压应变；杆与圆盘/孔位之间更接近离散约束、接触、间隙与可能滑移的系统。因此，完整复合截面 `Iz` 更像“4 根杆被刚性组合成整体梁”时的刚度上界，不能直接作为当前准静态弯矩项 `M = E*I*(theta/l)` 的默认材料口径。

验收诊断（20 samples, scale=0.3, PSO=24×40, 张力上限 2000N）：
- `configs/robot_true_material.yaml`（使用 `material_true_composite_centroid.csv`）：
  - `rms_rnorm.p90≈0.861`，且 `max_tension.p50/p90=2000`（长期打满）→ **在 0..2000N 约束下不可达**

这意味着：如果我们坚持使用“几何真实 Iz_true + 当前弯矩项口径 `M=EI*(θ/l)`”，则需要同步调整至少一项：
- 提高张力上限（若论文/实机允许 >2000N）
- 或修正/更换弯矩项模型口径（例如考虑滑移/间隙导致的等效 EI 显著更小）
- 或缩小采样范围（让 θ 更小，从而降低所需弯矩）

因此，为了先把“论文数据集复现管线”跑通并保持可达性（与论文张力范围一致），当前采用你确认的 **rods-only 口径**（每根 NiTi 棒只按其自身形心轴计算弯曲二次矩，不加平行轴项；4 根棒的弯矩直接相加）：
- 单杆：`I_single = π d^4 / 64`
- 4 杆等效：`Iz = 4 * I_single`
- 取 `d=1.6mm`：
  - `I_single ≈ 3.216990877e-13 m^4`
  - `Iz ≈ 1.286796351e-12 m^4`
- 取 `E_pa = 4.0e10`（40GPa，先用合理值跑通；后续可替换为你 SolidWorks/材料手册的真实 E）

后续如果你用 SolidWorks 的 Section Properties 得到装配体的 `Iz_mm4`，我们可以再把 `Iz_m4` 替换为“更真实”的值，并同步调整：
- 张力上限（若论文/实机允许更大）
- 或准静态弯矩项模型（若论文用的不是 `M=EI*(θ/l)` 这种口径）

### 验收
- `material.csv` 中 `E_pa>0` 且 `Iz_m4>0`（否则准静态弯矩项无意义）
- `python3 scripts/diagnose_solver.py --config configs/robot_generated.yaml --samples 20 --scale 0.3 --pso-iters 40 --pso-particles 24`：
  - 期望：`rms_rnorm.p90 <= 0.06` 且 `max_tension.p90 < 2000`（不长期打满）

### 记录/假设
- 若暂时未知：先用常见 Nitinol `E≈30~50GPa`，`d` 用实物/图纸估计，并明确记录为“暂定值”，后续替换

### 补充：残差阈值的现实可达性诊断
用 `scripts/diagnose_solver.py` 在当前假设下评估 PSO 可达到的 `rms_rnorm` 分布（scale=0.3，pso=24粒子×40迭代，20个样本）：
- `min≈0.0155, p50≈0.0298, p90≈0.0476, max≈0.0533`
因此把可行性阈值设置为 `0.06`（见 `configs/robot_generated.yaml`）。

---

## Step 4：小规模数据集跑通（200~2000 条）

### 操作
1) 小跑通（顺序，便于调试）：
   - `python3 scripts/generate_dataset.py --config configs/robot_smoke.yaml --num-samples 200 --workers 1 --max-tried 20000`
2) 放大到 2000（评估可行率/耗时）：
   - `python3 scripts/generate_dataset.py --config configs/robot_smoke.yaml --num-samples 2000 --workers 4 --max-tried 200000`
3) 质量版（使用更强 PSO 设置与阈值）：
   - `python3 scripts/generate_dataset.py --config configs/robot_generated.yaml --num-samples 2000 --workers 4 --max-tried 400000`

4) （新增口径）NiTi 杆“rods-only”弯曲刚度 + 每个 theta 限制 25°，跑 2000 条：
   - 采用：`Iz = 4 * (π d^4 / 64)`（每根棒只计自身形心轴弯曲，四根相加），`d=1.6mm`
   - 配置：`configs/robot_rods_only_25deg.yaml`
   - 运行：`python3 scripts/generate_dataset.py --config configs/robot_rods_only_25deg.yaml --num-samples 2000 --workers 4 --max-tried 300000`

5) （新增）基于统计结果：25° 下 `max_tension==2000N` 占比过高 → PSO 目标加入“大张力惩罚”，并把采样空间收缩到 20°：
   - 配置（验收用 2000 条）：`configs/robot_rods_only_20deg_penalty_2k.yaml`
   - 运行：`python3 scripts/generate_dataset.py --config configs/robot_rods_only_20deg_penalty_2k.yaml --num-samples 2000 --workers 16 --max-tried 400000`

### 验收
- 生成 `data/dataset.parquet` 与 `data/dataset_meta.parquet`
- 行数匹配
- 所有 `tension_*_n` ∈ [0,2000]
- `dataset_meta.parquet` 中 `rms_rnorm < threshold`（由 config 控制）
- `abs(theta_i) <= 25deg`（即 `<=0.4363323 rad`）
- 张力打满率：`P(max_tension==2000N)` 不应长期大于 20%（否则会导致大量样本不可用/边界过密）

### 结果（已完成）
- smoke 50 条（顺序）：成功生成 `data/smoke_out/dataset.parquet`，tension 范围 [0,2000]，无 NaN
- smoke 200 条（4 workers + resume）：成功生成 200 行，tension 范围 [0,2000]
- rods-only（25°，100k，baseline）：`data/rods_only_25deg_100k/dataset.parquet`（100000 行）
  - `P(max_tension==2000N) ≈ 54.772%`（需要改进）
- rods-only（20° + 张力惩罚，2k 验收）：`data/rods_only_20deg_penalty_2k/dataset.parquet`（2000 行）
  - `max|θ|≈20.0°`（满足 20° 约束）
  - `P(max_tension==2000N)=9.25%`（显著降低）
  - `rms_rnorm.max≈0.059881 < 0.06`（全部通过阈值）

### 记录/假设
- 若可行率过低：优先增大 PSO `iters/n_particles` 或缩小采样 `beta_ranges_rad`，并记录调整

---

## Step 5：全量生成 100,000 条（并行，支持断点续跑）

### 操作
- 使用已验收配置（20° + 张力惩罚）：`configs/robot_rods_only_20deg_penalty_100k.yaml`
- 命令（带日志）：
  - `python3 scripts/generate_dataset.py --config configs/robot_rods_only_20deg_penalty_100k.yaml --num-samples 100000 --workers 16 --max-tried 140000 --resume |& tee data/rods_only_20deg_penalty_100k/run.log`

### 验收（最终）
- 输出存在：
  - `data/rods_only_20deg_penalty_100k/dataset.parquet` 行数=100000
  - `data/rods_only_20deg_penalty_100k/dataset_meta.parquet` 行数=100000
  - `data/rods_only_20deg_penalty_100k/dataset_report.json` 存在，且 `accept_rate`、`elapsed_s` 合理
- 基本质量：
  - `abs(theta_i) <= 20deg`
  - `tension_i ∈ [0,2000]`
  - `dataset_meta.rms_rnorm.max < 0.06`
  - `P(max_tension==2000N) <= 15%`（目标；若明显高于此值，继续加大惩罚或收缩采样）
- 工作空间分布检查：
  - `python3 scripts/analyze_dataset.py --dataset data/rods_only_20deg_penalty_100k/dataset.parquet --meta data/rods_only_20deg_penalty_100k/dataset_meta.parquet --out-json data/rods_only_20deg_penalty_100k/analysis.json`

---

## Step 6：复现论文的 MLP 拟合（输入 xyz → 输出 42 维）

论文描述：输入为末端圆盘中心点 `x,y,z`，输出为 30 个 `θ_i` 与 12 个 `Tension_j`，MLP 结构为 3 hidden layers：128/64/32（见 `原论文.md` Fig.8 & 描述段落）。

### 操作
1) 选定数据集（建议用最终版 100k）：
   - `data/rods_only_20deg_penalty_100k/dataset.parquet`
2) 训练 MLP（会自动做 X/Y 标准化，并输出测试集指标）：
   - `python3 scripts/train_mlp.py --dataset data/rods_only_20deg_penalty_100k/dataset.parquet --robot-config configs/robot_rods_only_20deg_penalty_100k.yaml --out-dir runs/mlp_rods_only_20deg_penalty_100k --early-stopping`

### 验收
- 训练完成后 `runs/.../metrics.json` 存在，包含：
  - `rmse_theta_deg` / `rmse_tension_n`
  - 端点回代误差（用 FK(pred_theta) vs 输入 xyz）：`ee_pos_rmse_mm`, `ee_pos_p95_mm`, `ee_pos_max_mm`
- `runs/.../mlp.joblib` 存在，可用于后续推理/复现实验

### 记录/说明
- 当前训练脚本使用 scikit-learn 的 `MLPRegressor`（环境中已存在 `sklearn`），网络结构与论文一致；若后续需要完全对齐论文训练细节（学习率/epoch/损失/归一化），可在此基础上进一步锁定参数并记录。

According to a document (date not specified in the provided file),这篇论文里“生成数据集”的本质并不是在某个大型仿真器里做高频动力学仿真，而是：**用解析的运动学 + 准静态力矩平衡方程 +（Monte Carlo 采样 + PSO 优化）**去“算”出样本。这个流程完全可以在 Python 里稳定实现，所以我才会说：**如果你只想复现论文同款数据集（输入末端位姿/位置，输出关节角+绳索张力），Python 就够了**。下面我先解释“为什么”，再给你一个可以直接交给本地 codex cli 执行的、分阶段可验收的复现计划。

---

## 1) 为什么说“只用 Python 就能复现虚拟样机 + 数据集生成”

### 1.1 论文的数据集是“数值生成”，不是“物理引擎逐步仿真”

论文在“仿真数据采集/神经网络训练”部分明确：

* 虚拟样机模型只是把模型坐标系与机器人基坐标系对齐（轴向一致、原点重合），并在其上采集数据。
* 采集的**输入**是末端盘中心在基座标系下的齐次坐标 (^0p_{target}=[x,y,z,1]^T)，**输出**是 12 路驱动绳张力 + 30 个关节角。
  这意味着数据集不是从“仿真器时间步进积分”出来的，而是“给定构型 → 算出末端位置与静力平衡下的张力”。

### 1.2 论文采用“准静态”假设，主动弱化了动力学时间项

论文给出理由：机器人运动很慢（最大绳速 < 10 mm/min），因此可以认为处于静力/静矩平衡的准静态状态。
这直接把问题从“需要物理引擎做动态仿真”变成了“解静力平衡方程 + 优化”。

### 1.3 数据集生成算法本身就是“Monte Carlo + PSO”，天然适合 Python

论文的数据采集流程是：

* Monte Carlo 生成 (\beta=(\beta_1,\dots,\beta_6))
* 由 (\beta) 生成 30 个关节角 (\theta_i)（每段奇/偶关节角近似一致）
* 用运动学（DH 连乘）算末端位置 (^0p_{target})
* 再用准静态力矩平衡方程（z 轴分量）形成方程组并在外力为零时取 (F_{exp}=0, M_{F_{exp}}=0)
* 最后用 PSO 在“尽量满足平衡 + 最小化最大张力”下求 12 路张力
* 一共生成 100,000 组数据
  这一整套就是标准“数值建模 + 优化 + 批量采样”，Python（numpy/scipy/并行）是最合适的落地方式。

---

## 2) 用 Python 稳定复现“论文同款数据集”的可执行计划（给 codex cli）

下面这份计划目标非常明确：**输出一个包含 100,000 行样本的数据文件**，每行字段至少包括：

* 输入：(x,y,z)（或保留齐次 (x,y,z,1)）
* 输出：(\theta_1\ldots \theta_{30}) + (Tension_1\ldots Tension_{12})
  并满足张力上限约束 2000 N。

> 关键点：你有 SolidWorks 模型，它的最大价值是**提供几何与质量参数**（孔位、盘质量/质心、杆参数、段长 (l_i) 等）。论文的数据集生成依赖这些参数。

---

# Phase A：工程骨架与配置（先搭架子，保证可复现）

### A1. 建 repo 结构（codex 直接照做）

建议结构（最小但清晰）：

* `configs/robot.yaml`（机器人参数：几何/物性/摩擦/绳路由）
* `src/model/dh.py`（DH 变换与正运动学）
* `src/model/cable_geometry.py`（孔位变换、绳长计算）
* `src/model/tension_transmission.py`（绳-孔摩擦张力传递 Eq.42）
* `src/model/quasi_static.py`（重力矩、绳力矩、弯矩、轴摩擦矩、平衡残差 Eq.46）
* `src/opt/pso.py`（PSO）
* `scripts/generate_dataset.py`（主入口：采样→求解→落盘）
* `tests/test_sanity.py`（少量单元测试，保证稳定）

依赖建议（固定版本更稳）：`numpy, scipy, pandas, pyarrow, tqdm, pyyaml, numba(可选), joblib(并行可选)`。

### A2. `robot.yaml` 必要参数清单（来自论文 + SolidWorks）

论文明确：机器人三段、每段 D=10，总盘数 kD=30。每盘 12 个孔。

配置里至少需要：

1. 结构

* `k=3, D=10, kD=30`
* `l[0..30]`：(l_0) 是线性模组进给量，(l_i) 是相邻转轴距离，(l_{kD}) 是末端转轴到末端的长度
* `p_end_local`：({}^{30}p_{end})（末端盘中心在 {30} 坐标系下的齐次坐标）

2. 孔位（几何，来自 SolidWorks）

* `h_local[j]` 与 `h_local_prime[j]`：每个盘上 12 个孔的 proximal/distal 端坐标（在盘自身坐标系 {i} 下）。论文给出从 {i} 到 {0} 的变换公式。

3. 绳索穿过盘的数量/终止盘号
   论文给出 (N)（每根绳穿过的盘数）定义。

> 注意：论文在“文字描述的绳编号所属段”与“公式给出的 N/段内 j 集合”之间可能存在编号口径差异；最稳的做法是**把“每根绳的终止盘号/穿盘数”写在配置里**，后面所有力矩求和都按它来（而不是死信某一句文字）。

4. 质量/质心（来自 SolidWorks）

* `m[p]`、`com_local[p]`：第 p 个盘质量与质心（或全部盘相同也可）。重力矩计算用到 Eq.(19)-(22)。

5. Nitinol 杆弯矩参数

* `E[i]`、`Iz[i]`：弯矩项用 Eq.(27)。

6. 摩擦

* 绳-孔摩擦 `mu_c`：张力传递 Eq.(42)
* 轴-盘摩擦 `mu` 与轴半径 `r=0.004`：轴摩擦矩 Eq.(35)

7. 张力约束

* `tension_min=0`（论文说明绳只能拉不能压，张力非负）
* `tension_max=2000` N

---

# Phase B：实现运动学与绳几何（可单独验收）

### B1. 正运动学（DH）

* 用 Table 1 的 DH 参数规则实现：(\alpha_{i-1}) 取决于 i 奇偶，(a_{i-1}=l_{i-1})，(d_i=0)
* 总变换是连乘
* 末端点：(^0p_{target} = \Omega_{30}^0T \cdot {}^{30}p_{end})

**验收点（tests）**

* `theta=0` 时，末端应落在 x 轴方向的“总长度”上（由 l 累加决定）。
* 变换矩阵正交性检查（R^T R≈I）。

### B2. 孔位变换 + 绳长

* 由 Eq.(7)(8)把孔位变到基坐标
* 绳长用 Eq.(5)，并按每根绳的穿盘数 N 取和

**验收点**

* `theta=0` 时所有绳长为基准 `L0[j]`；微小弯曲时，拽紧一侧绳长应减小（符号检查）。

---

# Phase C：实现准静态力矩平衡残差（核心）

### C1. 平衡方程用 z 轴分量 Eq.(46)

论文把整体力矩平衡式 Eq.(43)取 z 分量得到 Eq.(46)。
你在代码里实现一个函数：

`residual(theta[30], T_base[12]) -> r[30]`

其中每个 `r[i]` 是第 i 个关节的 z 轴力矩残差。

### C2. 各项怎么计算（都能在 Python 里按公式写出来）

1. **重力矩**：Eq.(19)-(22)

* 需要把第 p 个盘的质心位置与重力从 {0} 变换到 {i}，然后做叉乘得到力矩。

2. **绳张力力矩**：Eq.(23)-(26)

* 张力方向是从 (h_{i,j}) 指向 (h'_{i-1,j})
* 先得到 ({}^i h_{i-1,j}')（Eq.25）再归一化方向向量
* 力矩 = ((h_{i,j}-O_i)\times F)，取 z 分量

3. **Nitinol 杆弯矩**：Eq.(27)

* 弯矩 z 分量：(\frac{\theta_i}{l_i}E_i I_{z_i})

4. **轴摩擦矩**：先由力平衡 Eq.(34)求支撑力，再由 Eq.(35)求摩擦矩

* 方向论文说依赖历史状态

  * 为了“离线生成数据集”的稳定性（没有历史），建议在代码里采用**“摩擦矩总是抵抗当前合力矩趋势”**：

    * 先算不含轴摩擦的 z 向合力矩 `Mz_raw`
    * 再让 `Mz_fric = -sign(Mz_raw) * mu * ||Fs_i|| * r`
      这能避免你为了造历史而引入额外状态机。

5. **外力/外矩**：数据集生成阶段可按论文置零

6. **每个 i 要对哪些绳求和？**
   论文给出分段的 j 集合（Eq.44）。
   但为了避免编号口径问题，建议你的实现以配置为准：

* 配置 `cable_end_disk[j]`（这根绳终止在哪个盘/穿过多少盘）
* 对于关节 i，只对满足 `cable_end_disk[j] >= i` 的绳计算力矩（更通用，也能兼容 Eq.44 的形式）。

---

# Phase D：张力传递 + 用 PSO 求 12 路基座张力（生成标签）

### D1. 绳-孔摩擦张力传递 Eq.(42)

论文的张力传递为递推：(F_{CT_{i+1,j}}) 由 (F_{CT_{i,j}}) 与 (\theta_i,\theta_{i+1}) 给出。

* 你要从基座张力 (F_{CT_{0,j}})（即输出标签 `Tension_j`）开始一路递推到该绳的终止盘。
* Case1/Case2 的选择论文说可由历史状态推断。为了离线数据集稳定，建议这样选：

  * 先用绳长变化 (\Delta L_j = L_j(\theta)-L_j(0)) 判断：

    * (\Delta L_j < 0)：绳被收紧（向基座运动）→ 用“向近端运动”的 case
    * (\Delta L_j > 0)：绳被放松（向末端运动）→ 用另一个 case
  * 把“case 与方向”的映射写在配置里（可切换），这样你一旦和论文图 6 的定义对齐，就可以固定下来。

### D2. PSO 要解什么问题？

论文给出：

* 由 Eq.(46) 得到 30 个关节力矩平衡方程（写成 (f(\theta_i,Tension_j)=0)）
* 为减小磨损，最小化最大张力
* 用 PSO 求 12 路张力

**工程上最稳的落地方式**：把“方程 = 0”变成**残差平方惩罚项**，做有界优化：

* 决策变量：`T_base[12]`，边界 `[0, 2000]`
* 目标函数（建议）：
  [
  J(T)= w\cdot \frac{1}{30}\sum_{i=1}^{30} r_i(\theta,T)^2 + \lambda\cdot \max_j T_j
  ]

  * `r_i` 来自 Phase C 的残差函数
  * `w` 取很大（强迫满足平衡），`λ` 取小一些（在满足平衡的前提下压低最大张力）

**PSO 参数建议（写进配置，便于复现实验）**

* `n_particles`: 64 或 128
* `iters`: 150~300（先小样本调通再上大规模）
* `inertia, c1, c2`: 常见取值（如 0.7, 1.4, 1.4），并固定随机种子保证可复现
* early stop：`best_cost` 或 `mean(r^2)` 达到阈值就停

---

# Phase E：按论文方式采样 100,000 组并落盘（最终产物）

### E1. Monte Carlo 采样 (\beta) 并生成 (\theta)

* 采样 (\beta=(\beta_1,\dots,\beta_6))
* 用 Eq.(50) 映射成 30 个关节角（每段奇/偶关节角取同一参数）

> (\beta) 的取值范围论文片段未直接给出。最稳做法是：
>
> * 从 SolidWorks 的机械极限（干涉、孔位可达性）导出每段的最大弯曲角范围；
> * 或用论文 Table2 极限姿态的量级做初始范围，然后逐步扩大并检查是否出现大量不可行样本（PSO 残差降不下去、张力超限等）。

### E2. 每个样本的生成流程（脚本里写成一个纯函数，方便并行）

对每个采样 (\beta)：

1. `theta = beta_to_theta(beta)`（Eq.50）
2. `p = forward_kinematics(theta)` 得到 `x,y,z`（Eq.51）
3. `T = solve_tensions_pso(theta)`（Eq.52+53）
4. 质量门控：

   * `0 <= T_j <= 2000`
   * `mean(r^2) < tol`（你自设，比如 1e-6~1e-4，取决于量纲）
   * 不满足就丢弃该 beta 并重采（直到凑够 100,000 行有效样本）

### E3. 输出文件格式（建议 Parquet + metadata）

建议保存：

* 主数据：`dataset.parquet`（列：x,y,z, theta1..theta30, tension1..tension12）
* 辅助：`dataset_meta.parquet`（列：beta1..beta6, residual_norm, pso_seed, iters_used, case_flags 等）
  这样你将来追溯某条异常样本会非常省事。

### E4. 与论文对齐的规模

论文明确“一共 100,000 组数据”——你的脚本最后就以**有效样本计数达到 100,000**作为终止条件（不是采样次数到 100,000）。

---

## 3) 给 codex cli 的“逐条任务清单”（照单执行即可）

你可以把下面直接复制给本地 codex cli，让它按步骤实现：

1. 初始化 Python 工程与依赖（requirements/pyproject 固定版本），建立目录结构（configs/src/scripts/tests）。
2. 写 `configs/robot.yaml` 模板：把 k=3、D=10、张力上限 2000N、轴半径 4mm、以及需要你从 SolidWorks 填的孔位/质量/质心/E/Iz/摩擦系数等字段全部列出来（先填占位符）。
3. 实现 DH 正运动学（Table1 + 连乘 Eq.4），并实现 `forward_kinematics(theta)->p_target`（Eq.51）。
4. 实现孔位变换 Eq.(7)(8) 与绳长 Eq.(5)，并用 Eq.(6)/配置的穿盘数决定求和范围。
5. 实现张力传递 Eq.(42)（可配置 case 选择策略），输出每个 i、j 的 (F_{CT_{i,j}})。
6. 实现准静态残差 `residual(theta,T_base)`：

   * 重力矩 Eq.(19)-(22)
   * 绳力矩 Eq.(23)-(26)
   * 弯矩 Eq.(27)
   * 支撑力 Eq.(34) 与轴摩擦矩 Eq.(35)
   * 平衡用 Eq.(46)，外力外矩置零 Eq.(47)(48)。
7. 写 PSO（或用现成库但要固定随机种子），目标函数按“残差平方 + 最大张力”构造，并加上 `[0,2000]` 边界。对齐论文“最小化最大张力 + 用 PSO 求解”描述。
8. 写 `generate_dataset.py`：

   * Monte Carlo 采样 beta（6 维）
   * 用 Eq.(50) 映射成 theta
   * 算 p_target
   * PSO 求 Tension
   * 做可行性筛选，累计到 100,000 行
   * 输出 parquet
9. 写 tests：至少验证

   * DH 连乘正确性（R 正交）
   * Eq.(5) 绳长计算在 theta=0 时与基准一致
   * 目标函数在边界外会被惩罚（或 PSO 不会越界）
10. 先跑小规模（比如 100~1000 行）看残差分布/张力是否合理，再全量生成 100,000 行并保存。

---

## 4) 你真正需要“从 SolidWorks 拿到”的东西（否则无法对齐论文数据）

只要你要“同款数据集（同语义）”，下面这些一定要有（否则 Python 再完美也只能算“另一个机器人”的数据）：

* 各盘坐标系下：12 个孔的 (h_{i,j}, h'_{i,j}) 坐标（或至少孔在盘面上的圆周半径与角度分布）
* 每盘质量与质心位置（用于重力矩）
* 段长 (l_i)、末端点定义 ({}^{30}p_{end})
* 杆参数 (E, I_z)（用于弯矩）
* 摩擦系数 (\mu,\mu_c)（用于轴摩擦与绳-孔摩擦传递）

---

如果你愿意把这套计划再“落到更具体的工程实现粒度”（例如：我把 `robot.yaml` 的字段逐项写成模板、把每个模块的函数签名/输入输出/中间变量命名约定都定死，甚至把 PSO 的超参数与 early-stop 条件写成可直接复制的默认值），我也可以继续往下把“codex 应该生成哪些文件、每个文件里应该有哪些函数、测试怎么写”整理成更像 PRD/开发任务书的形式——但即使到这一步，你现在已经可以让 codex cli 按上面的 Phase A~E 逐步实现并最终产出数据集了。

## 实验目的

**具有显著自重、强耦合、驱动力大**的缆绳驱动连续体机器人在路径跟随任务中，如何在保证精度的同时做到实时、并降低对真实机器人“暴力采数”导致的磨损/损伤风险。

论文逻辑链路：**运动学建模 → 准静态动力学推导 → 运动学-静力学耦合求解 → 虚拟样机生成数据集 → 训练神经网络学逆模型 → 建立从任务空间到驱动空间的控制方法**。

在 训练神经网络逆运动学模型 中，论文直接采用了MLP方法对数据集进行训练，在审稿意见中审稿人提出为什么不使用像LSTM这样的更新模型。虽然本实验中，要学习的是一个**静态、多输出回归**问题，并不是LSTM的适用范围，然而，确实需要针对于静态、多输出回归问题的模型选择，仍然需要补充一个主流机器模型性能对比的实验进行逻辑补充。

## 实验方案

### 训练集

训练集来自原文虚拟样机+Monte Carlo 生成，共 100,000 组样本

- 输入是末端盘中心点在基坐标系下的三维笛卡尔坐标
- 输出为 **30 个关节角 + 12 路缆绳张力（共 42 维）**
- 仿真数据生成阶段施加了最大驱动力（张力）约束 **2000 N**

>数据划分可以分为两类，一类是IID随机划分，一类是将工作空间的极端位姿单独分为一类（末端点到基座距离$r=\|p\|$较大的分为一类）
>
### 预处理与损失函数设计

1. **输入归一化**：对 $[x,y,z]$做 z-score（均值 0 方差 1），用训练集统计量。

2. **输出分组归一化**：
 - 角度组 $\theta$：z-score
 - 张力组 $T$：z-score 或者先除以 2000 再 z-score

3. 分组加权损失(正面回应“欠拟合分析不足”)
 - $\mathcal{L}=\lambda_\theta \cdot \mathrm{MSE}(\hat{\theta},\theta)+\lambda_T \cdot \mathrm{MSE}(\hat{T},T)$, $\lambda_\theta,\lambda_T$可以取 1:1

4. 物理约束违背惩罚
 1. 统计 $\hat{T}_j<0$或$\hat{T}_j>2000$ 的比例；也可在训练里加软惩罚项：
 $\mathcal{L}_{phys}=\alpha\sum_j \big[\max(0,-\hat{T}_j)+\max(0,\hat{T}_j-2000)\big]^2$

### 横向对比模型

主流baseline模型：

1. MLP(现有模型)：3×FC(128-64-32) → 42
2. **MLP-Large（检验欠拟合）**：4×FC(256-128-64-32) → 42
3. **LSTM**：用于回应审稿意见的核心对比组（不必要，因为该模型并不适配本问题）
4. SVR
5. GPR
6. 树模型集成:随机森林、梯度树
7. KNN回归/插值
8. **RBFNN（径向基神经网络）**：长期以来就是 MLP 的经典对照组；在连续体/软体机器人运动学学习建模中经常出现。
9. **ANFIS/CANFIS（神经模糊系统）**：在连续体机器人运动学建模对比研究中也很常见，并且有论文明确把 **MLP、RBF、CANFIS、SVR** 放在同一张对比表里评估误差与训练时间等指标。
10. **ELM（Extreme Learning Machine）/单隐层前馈网络变体**：在 IK 文献中也能看到“单隐层前馈网络 + 快速求权重”的思路（作为轻量基线很合适）

#### 如何使用时序模型LSTM

**配置-1（单点输入，序列长度 L=1）**：
 把每个样本看成长度为 1 的序列喂给 LSTM/GRU。  
 目的：证明“在静态问题上，序列模型没有优势/甚至更慢”。

**配置-2（路径序列输入，序列长度 L>1，推荐 L=5 或 10）**：
 可以用 相邻路径点序列 构造训练样本，输入为前序坐标点，输出为下一个坐标点对应的逆运动学输出。

预期结果：MLP 与 LSTM/GRU 精度相当或更好、且推理更快、张力约束违背更少  
→ 结论：该任务属于静态非线性映射，MLP 结构更匹配、复杂时序模型引入额外开销

### 性能指标

1. 输出空间指标
 1. 关节角
  1. $MAE_\theta(rad)$
  2. $RMSE_\theta(rad)$
 2. 张力
  1. $MAE_T(N)$
  2. $RMSE_T(N)$
 3. $\%\{\hat{T}_j<0\}$
 4. $\%\{\hat{T}_j>2000\}$
2. 路径跟随误差
3. 实时性指标

---

## 实验落地（本仓库脚本）

为避免 Python 版本差异影响，补充实验的 baseline 训练/评估脚本采用**独立实现**，不依赖 `src/quasi_exp`。

### 推荐运行环境
- `dante_env`（默认）：统一训练入口，含 TensorFlow-GPU；并补齐 `pyarrow + lightgbm`
- `quant_env`（回退）：当 `dante_env` 不可用时使用

训练前建议先做环境预检：
```bash
conda run -n dante_env python scripts/check_ml_env.py \
  --require-packages tensorflow,pyarrow,lightgbm,sklearn,numpy,pandas \
  --require-tf-gpu
```

### 运行命令（IID 随机划分）
在 100k 尚未生成完成前，可先用 2k 小样本跑通全流程（训练 + 出图）作为验收：
```bash
conda run -n dante_env python scripts/baselines/run_baselines.py \
  --dataset data/rods_only_20deg_penalty_2k/dataset.parquet \
  --robot-config configs/robot_rods_only_20deg_penalty_2k.yaml \
  --out-dir runs/baselines_2k_quant_smoke \
  --models mlp,mlp_large,lgbm,knn \
  --save-preds 500 --save-curves \
  --n-jobs 2

conda run -n dante_env python scripts/baselines/plot_baselines.py \
  --out-dir runs/baselines_2k_quant_smoke
```

以最终 100k 数据集为例（生成完成后）：
```bash
conda run -n dante_env python scripts/baselines/run_baselines.py \
  --dataset data/rods_only_20deg_penalty_100k/dataset.parquet \
  --robot-config configs/robot_rods_only_20deg_penalty_100k.yaml \
  --out-dir runs/baselines_rods_only_20deg_penalty_100k \
  --seed 20260207 --val-size 0.1 --test-size 0.1 \
  --models mlp,mlp_large,rf,lgbm,knn \
  --save-preds 2000 --save-curves \
  --n-jobs -1 \
  --split iid
```

可选：工作空间外推评估（把 `r=||p||` 最大的样本留作 val/test）：
```bash
conda run -n quant_env python scripts/baselines/run_baselines.py \
  --dataset data/rods_only_20deg_penalty_100k/dataset.parquet \
  --robot-config configs/robot_rods_only_20deg_penalty_100k.yaml \
  --out-dir runs/baselines_rods_only_20deg_penalty_100k_radius_split \
  --seed 20260207 --val-size 0.1 --test-size 0.1 \
  --models mlp,mlp_large,lgbm,knn \
  --save-preds 2000 --save-curves \
  --n-jobs -1 \
  --split radius
```

生成图表（性能指标对比 + 拟合可视化：loss/pearson/残差/pred-vs-true）：
```bash
conda run -n dante_env python scripts/baselines/plot_baselines.py \
  --out-dir runs/baselines_rods_only_20deg_penalty_100k
```

可选：加入 SVR/GPR（会自动子采样训练，耗时显著增加）：
```bash
conda run -n dante_env python scripts/baselines/run_baselines.py \
  --dataset data/rods_only_20deg_penalty_100k/dataset.parquet \
  --robot-config configs/robot_rods_only_20deg_penalty_100k.yaml \
  --out-dir runs/baselines_rods_only_20deg_penalty_100k_heavy \
  --models mlp,mlp_large,rf,lgbm,knn,svr_rbf,gpr_rbf \
  --svr-max-train 20000 --gpr-max-train 800 \
  --save-preds 2000 --save-curves \
  --n-jobs -1
```

### 输出与验收
每个模型会在 `--out-dir/<model_key>/metrics.json` 写入指标；汇总在 `--out-dir/all_metrics.json`。
图表会输出到 `--out-dir/plots/`。
验收时重点检查：
- `theta_mae_deg / theta_rmse_deg`
- `tension_lt0_ratio / tension_gt_tmax_ratio`
- `ee_pos_p95_mm`（端点回代误差，越小越好）

> 备注：GPR 的训练复杂度约为 $O(n^3)$（按每个输出维度单独拟合更慢），在 100k 上通常不可行；建议只做小数据子采样基线，并在文中说明原因。

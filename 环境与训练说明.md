# 环境与训练说明（base / dante_env / 后续新环境建议）

## 1. 当前本仓库“生成数据集 + sklearn 训练”的环境

目前我在机器上确认到：
- 默认环境：`CONDA_DEFAULT_ENV=base`
- Python：`/root/anaconda3/bin/python3`（3.11.x）
- 该环境已具备本仓库所需依赖：
  - 数据生成：`numpy/scipy/pandas/pyarrow/PyYAML/tqdm`
  - sklearn 训练：`scikit-learn/joblib`

因此：**先用 base 直接复现论文 MLP（sklearn）是可行的**。

> 复现脚本：`scripts/train_mlp.py`（输入 xyz → 输出 42 维：30θ + 12T）。

---

## 2. dante_env 当前状态与修复

你提到的 `dante_env` 位于：`/mnt/ML_projects/conda_envs/dante_env`。

### 2.1 发现的问题
最初该环境在 Linux 上无法 `conda activate/conda run` 正常工作，原因包括：
- `bin/python`/`bin/python3` 是 Windows `.lnk`（导致 “Exec format error”）
- 多个运行时 so 文件是“很小的占位文件”（`file too short`），例如 `libz.so.1`、`libffi.so.8`、`libstdc++.so.6`、`libgfortran.so.5`、`libquadmath.so.0`、`libgomp.so.1`

### 2.2 已做修复
已在该环境内完成以下修复，使其能在 Linux 上正常激活并运行：
1) 修复 python 入口（用真实解释器替换 `.lnk`）：
   - 脚本：`scripts/fix_conda_env_python_links.sh`
2) 修复关键 runtime so 的错误链接（把 “tiny file” 替换成正确 so 的软链接）：
   - 脚本：`scripts/repair_dante_env_runtime_libs.sh`

### 2.3 修复后的验收方式
在 shell 中执行：
- `source /root/anaconda3/etc/profile.d/conda.sh`
- `conda activate /mnt/ML_projects/conda_envs/dante_env`
- `python -c "import numpy, sklearn; print('ok', numpy.__version__, sklearn.__version__)"`

### 2.4 dante_env 读取数据集（更新）
当前训练入口已切换为默认 `dante_env`，并要求该环境具备：
- `tensorflow`（且可见 GPU）
- `pyarrow`（读取 parquet）
- `lightgbm`（baseline 中 `lgbm` 模型）

建议先运行：
- `conda run -n dante_env python scripts/check_ml_env.py --require-packages tensorflow,pyarrow,lightgbm,sklearn,numpy,pandas --require-tf-gpu`

---

## 3. 后续建议：创建一个“稳定可复现”的新 ML 环境

考虑到：
- dante_env/quant_env 可能包含历史依赖、杂项包、或与本仓库 Python 版本不一致
- 后续还要跑更多 ML 模型（可能需要 torch/tensorflow/pyarrow 等组合）

建议新建一个专用环境（示例名：`quasi_ml`），用于：
- 读取 parquet 数据集（需要 `pyarrow`）
- 训练 MLP 与后续深度学习模型（torch 或 tensorflow）

### 3.1 推荐新环境（Python 3.11 + 本仓库依赖）
示例（按需二选一框架）：
```bash
source /root/anaconda3/etc/profile.d/conda.sh
conda create -p /mnt/ML_projects/conda_envs/quasi_ml python=3.11 -y
conda activate /mnt/ML_projects/conda_envs/quasi_ml

# 安装本仓库依赖（含 sklearn 训练）
pip install -r requirements.txt

# 或安装为可编辑 + 可选依赖（若你更偏好）
pip install -e '.[ml,dev]'

# (可选) torch 或 tensorflow 按你后续模型选装
```

### 3.2 迁移参考：导出的环境清单
为了后续“复制 dante_env / quant_env 的关键依赖”，已导出：
- `env_specs/dante_env.full.yml`
- `env_specs/quant_env.full.yml`
- `env_specs/dante_env.from_history.yml`
- `env_specs/quant_env.from_history.yml`

你可以基于这些 yml 手工挑选要合并到 `quasi_ml` 的包（更推荐“挑关键包”，不要直接无脑 merge 两个 full yml）。

---

## 4. 现存环境的“可复用能力矩阵”（用于补充实验选型）

| 环境 | Python | pyarrow(读parquet) | sklearn | lightgbm | torch | tensorflow | 适合做什么 |
|---|---:|---:|---:|---:|---:|---:|---|
| `base` | 3.11 | ✅ | ✅ | ❌ | ❌ | ❌ | 数据生成、sklearn 版 MLP/baseline、统计分析 |
| `quant_env` | 3.9 | ✅ | ✅ | ✅ | ✅(CPU版) | ❌ | 兼容回退环境（当 dante_env 失效时） |
| `dante_env` | 3.10 | ✅ | ✅ | ✅ | ❌ | ✅(GPU可用) | 当前默认训练环境（baseline + TensorFlow/GPU 预检） |
